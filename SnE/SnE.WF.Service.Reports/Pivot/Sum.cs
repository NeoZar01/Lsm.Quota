using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using System.Web;
using System.Data.Entity;

namespace DoE.Models.dp_mdata_dist.Publications
{
    public class Sum
    {

        //public async static Task<Row> AggregateAsync(ft_SchoolsPrDistrictPrSector d)
        //{

        //    var row = new Row(d) {
        //          intPrimaryPub = (from r in When.tablerows select r).Sum(c => c.intPrimaryPub) ,
        //          intPrimaryInd = (from r in When.tablerows select r).Sum(c => c.intPrimaryInd),
        //          intSecondaryPub = (from r in When.tablerows select r).Sum(c => c.intSecondaryPub),
        //          intSecondaryInd = (from r in When.tablerows select r).Sum(c => c.intSecondaryInd),
        //          intCombinedPub = (from r in When.tablerows select r).Sum(c => c.intCombinedPub),
        //          intCombinedInd = (from r in When.tablerows select r).Sum(c => c.intCombinedInd),
        //          intIntermediatePub = (from r in When.tablerows select r).Sum(c => c.intIntermediatePub),
        //          intIntermediateInd = (from r in When.tablerows select r).Sum(c => c.intIntermediateInd)
        //    };


        //    return row;
        //}


    }
}